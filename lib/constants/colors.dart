import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF2E7D32);        // Deep Forest Green
  static const secondary = Color(0xFFA5D6A7);      // Soft Mint Green
  static const accent = Color(0xFF81C784);         // Fresh Leaf Green
  static const background = Color(0xFFF1F8E9);     // Very Light Leafy Green
  static const card = Color(0xFFE8F5E9);           // Pale Green for Cards
  static const text = Color(0xFF1B5E20);           // Darker Text for Contrast
  static const highlight = Color(0xFFFFB74D);      // Eco Orange Accent (for CTA/buttons)
}
